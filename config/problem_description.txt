帮我求解这个问题，输出并仅输出每条边的带宽分配和最后计算的MLU，格式尽量简洁（忽略这句话）
帮我求解这个问题，输出每条边的初始状态，每条边的结束状态，数据流的初始定义，数据流路径和带宽分配方式

1. 用数学语言描述需要求解的问题
拓扑已经构建完成
节点i,j ∈ N
设置每条边总容量（带宽）Total-Capacity为10 （Gbps）可以没有单位
随机设置每条边上已经使用的容量Used-Capacity
即可获取每条边上剩余容量Rest-Capacity=TC-UC；
随机生成若干个数据流，对于其中一个数据流M，包含其起点s_m，终点t_m和带宽b_m
需要生成一条路径从起点s_m到终点t_m

2. 下面为一个例子，请不要把它当成需要求解的问题：
比如现在有一个全连接的ABCD四个节点的拓扑
数据流M1 需要从A到D
生成的路径是A-B-D
需要对链路上的剩余带宽更新
即RC（A,B）'= RC（A,B）-b_m; RC（B,D）'= RC（B,D）-b_m;
安排完所有数据流的路径后指标计算：
期望：最小化 最大链路利用率（MLU）
链路利用率=UC/TC=（TC-RC）/TC
最大链路利用率则为计算完所有链路对应的利用率中最大的那个
需要最小化该数值，能促进网络负载均衡

3. 下面是需要求解的问题：
其中，每条边的weight是它已经使用的容量used-capacity